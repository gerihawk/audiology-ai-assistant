# infra/

Reservado para artefactos de infraestructura local que no encajen
directamente en `docker-compose.yml` (por ejemplo, scripts de
inicialización de PostgreSQL o configuración de un proxy inverso en fases
futuras). Vacío durante la Fase 1.
